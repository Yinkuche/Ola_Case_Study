sqoop job

--meta-connect jdbc:hsqldb:hsql://localhost:16000/sqoop


--create timeJob
-- import

--connect jdbc:mysql://localhost/CDW_SAPP


--driver com.mysql.jdbc.Driver 

--query 'SELECT CONCAT(YEAR,LPAD(MONTH,2,0), LPAD(DAY,2,0)), DAY, MONTH, QUARTER(CONCAT(YEAR,LPAD(MONTH,2,0), LPAD(DAY,2,0))), YEAR
 
FROM CDW_SAPP_CREDITCARD WHERE $CONDITIONS'

--split-by YEAR

--target-dir /user/maria_dev/Credit_Card_System/CDW_SAPP_D_TIME/
--fields-terminated-by ','



